- form_tag user_journal_path(@page), :method => :put do
  != text_area 'journal', 'content', :class => 'long'
  %button{:class => 'submit', :type => 'submit'}= :update.l
  %a{:href => '#', :class => 'cancel'}= :cancel.l