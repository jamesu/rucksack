<%= form_tag( page_list_path(@page, @list), :method => :put) %>
  <%= text_field 'list', 'name', :class => 'autofocus long' %>
  <button class="submit" type="submit"><%= :rename_list.l %></button>
  <a href="#" class="cancel"><%= :cancel.l %></a>
</form>
</div>