demo_mode: false

email:
  delivery: test
  smtp:
    address: localhost
    domain: localhost
    username: myuser
    password: mypass
    authentication: login
  sendmail:
    location: "/usr/sbin/sendmail"
    arguments: "-t -t"
  inbox:
    address: localhost
    username: myuser
    password: mypass
    ssl: false
    port: 110
